const car = {
    "make":"ferrari",
    "model":"GT-4",
    "year":"2000"
};

function display(car){
    console.log(`made by : ${car.make}`);
    console.log(`model : ${car.model}`);
    console.log(`made in year : ${car.year}`);
}

display(car)